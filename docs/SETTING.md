# Setting book — rtos-firmware-atlas

Use the closed-loop stages to drive paper experiments, daily ops, and lab repro.
Prefer `lab-usable` before `hw-required`. Cross-link siblings instead of mixing domains.
Offense entries are research pointers only.
